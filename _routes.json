{
  "version": 1,
  "include": ["/*"],
  "exclude": [
      "/api/*",
      "/get_env/*"  // 新增排除规则
  ],
  "routes": [
      {
          "path": "/get_env/*",
          "function": "get_env"
      }
  ]
}
